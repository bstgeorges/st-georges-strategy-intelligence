# RFC 5545 requires CRLF line endings in iCalendar files.  Treat the carriage
# return as a line terminator, not trailing whitespace, during Git checks.
tools/reg-scan/docs/horizon.ics whitespace=cr-at-eol
